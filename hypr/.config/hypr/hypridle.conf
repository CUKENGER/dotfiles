general {
    lock_cmd = hyprlock
    before_sleep_cmd = pkill hyprlock; hyprlock
    after_sleep_cmd = hyprctl dispatch dpms on
}

listener {
    timeout = 3600
    on-timeout = systemctl suspend
    on-resume = hyprctl dispatch dpms on; hyprctl reload
}
